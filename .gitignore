node_modules/
*.log

# Local SQLite database (lives on the server's persistent disk, never in git)
data/

# Local environment / secrets
.env

# Obsidian per-user UI state (shared vault settings in notes/.obsidian stay in git)
notes/.obsidian/workspace.json
notes/.obsidian/workspace-mobile.json
